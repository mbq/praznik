#' @return A list with two elements: \code{selection}, a vector of names of the selected features in the selection order, and \code{scores}, a vector of corresponding feature scores.
#' Both vectors will either have a length \code{k} or zero, when all features turn out to have zero mutual information with the decision.


